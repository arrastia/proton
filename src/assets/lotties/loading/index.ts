import type { Animation } from 'components/AnimatedIcon/@types/AnimatedIcon.types';
import loading from '../loading.json';

export default { animationData: loading, animationKey: 'loading' } as Animation;
