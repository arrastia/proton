import { colors, corporateColors, gradients } from './colors';
import { shadows } from './shadows';

export const base = {
  borderRadius: '7px',
  colors,
  corporateColors,
  gradients,
  shadows
};
